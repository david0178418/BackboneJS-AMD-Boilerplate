(function() {
	"use strict";
	
	define([
			'underscore',
			'backbone'
		],
		function(_, Backbone) {
			
			return Backbone.Collection.extend({
				initialize : function() {
					
				}
			});
		}
	);
})();
