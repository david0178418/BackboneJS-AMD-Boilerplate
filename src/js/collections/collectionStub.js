(function() {
	"use strict";
	
	define([
			'underscore',
			'backbone'
		],
		function(Backbone) {
			
			return Backbone.Collection.extend({
				initialize : function() {
					
				}
			});
		}
	);
})();
